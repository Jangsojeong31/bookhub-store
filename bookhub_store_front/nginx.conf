server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        # 모든 요청을 index.html로 라우팅 (SPA 지원)
        try_files $uri /index.html;
    }

    location /assets/ {
        # 정적 파일은 그대로 제공
        try_files $uri =404;
    }
}
